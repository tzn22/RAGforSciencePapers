[build]
  builder = "NIXPACKS"

[build.environment]
  # ✅ ФИКС mise: используем STABLE Python 3.12
  PYTHON_VERSION = "3.12.7"

[deploy]
  startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
